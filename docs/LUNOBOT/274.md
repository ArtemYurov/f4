# Лунобот-2 (instance a721a6d1487257292ae00780; LNX): тикет #274

## Статус

- [x] Полностью прочитаны тело тикета, вся доступная переписка и все доступные изображения. Подтверждено, что в этом срезе нужны именно два способа переноса текущего профиля: Copy и Move.
- [x] Учтено принятое автором предложение: режим выбирается явно; Move удаляет исходный профиль только после успешного переноса, а ошибка переноса не переписывает файл режима. Перед переносом проверяются конфликты в целевом профиле, чтобы частичный неперезаписывающий Copy не стал потерей данных.
- [x] В диалоге Options → Portable mode вместо постоянного флажка добавлен выпадающий выбор Copy/Move.
- [x] Copy сохранил безопасное неперезаписывающее поведение и не переносит старые crash logs; Move переносит и crash logs, а исходный профиль удаляется только после переноса и записи нового режима.
- [x] Добавлены регрессионные проверки для отказа до удаления источника и для переноса crash logs.
- [ ] Hosted CI для этого среза ещё не завершён.
- [ ] Окончательное подтверждение автора и закрытие тикета не выполняются Луноботом.

## Границы среза

- Контекстная справка о примерах ini/logs, раскладке профиля и Lua оставлена для следующего атомарного среза.
- Реализация Lua в этот срез не входит.

## Проверка

- Локальные сборки и тесты не запускались согласно инструкции Лунобота.
- Перед отправкой PR выполнены только форматирование Go-файлов и проверка `git diff --check`.
