# Лунобот-2 (instance a721a6d1487257292ae00780; LNX): тикет #274

## Статус

- [x] Полностью прочитаны тело тикета, вся доступная переписка и все доступные изображения. Подтверждено, что в этом срезе нужны именно два способа переноса текущего профиля: Copy и Move.
- [x] Учтено принятое автором предложение: режим выбирается явно; Move удаляет исходный профиль только после успешного переноса, а ошибка переноса не переписывает файл режима. Перед переносом проверяются конфликты в целевом профиле, чтобы частичный неперезаписывающий Copy не стал потерей данных.
- [x] В диалоге Options → Portable mode вместо постоянного флажка добавлен выпадающий выбор Copy/Move.
- [x] Copy сохранил безопасное неперезаписывающее поведение и не переносит старые crash logs; Move переносит и crash logs, а исходный профиль удаляется только после переноса и записи нового режима.
- [x] Добавлены регрессионные проверки для отказа до удаления источника и для переноса crash logs.
- [ ] Hosted CI для этого среза ещё не завершён.
- [ ] Окончательное подтверждение автора и закрытие тикета не выполняются Луноботом.

## Границы среза

- Контекстная справка о примерах ini/logs, раскладке профиля и Lua оставлена для следующего атомарного среза.
- Реализация Lua в этот срез не входит.

## Проверка

- Локальные сборки и тесты не запускались согласно инструкции Лунобота.
- Перед отправкой PR выполнены только форматирование Go-файлов и проверка `git diff --check`.

## Follow-up slice 2 — context help

- [x] Расширена английская тема `PortableSettings`: добавлены `f4.example.ini`,
  layout каталога `Profile`, `logs/debug.log`, `crashes` и `Macros/scripts`.
- [x] Добавлен русский вариант той же темы и ссылка на него из индекса справки.
- [x] Регрессионная проверка проверяет наличие новых обязательных разделов в
  зарегистрированной теме.
- Реализация Lua-макросов не менялась; срез только документирует уже доступные
  пути и команду перечитывания скриптов.
- Локальные сборки и тесты для этого среза намеренно не запускались; выполнена
  проверка `git diff --check`.

## Follow-up slice 3 — horizontal dialog resize

- [x] Высота диалога настроек portable теперь фиксирована; его resize работает
  только по ширине.
- [x] Строки путей, примечания и строка выбора Copy/Move получают актуальную
  ширину через адаптивную раскладку; хвост длинного пути пересчитывается после
  изменения размера.
- [x] Добавлена регрессионная проверка: вертикальная граница не меняется при
  горизонтальном resize.
- [ ] Hosted CI для этого среза ещё не завершён.

Локальные сборки и тесты не запускались согласно инструкции Лунобота.

## Follow-up slice 4 — centered horizontal dialog resize

- [x] При изменении ширины диалог Portable mode сохраняет исходный горизонтальный центр: левая и правая границы смещаются симметрично.
- [x] Текст остаётся привязанным к левому краю, кнопки — центрированными средствами существующей адаптивной раскладки.
- [x] Регрессионная проверка проверяет обе границы, неизменный центр и фиксированную высоту.
- [ ] Hosted CI для этого среза ещё не завершён.

Локальные сборки и тесты не запускались согласно инструкции Лунобота.