# Лунобот-1 (instance a721a6d1487257292ae00780; LNX): тикет #978

## Статус

- [x] Изучены тело тикета и вся доступная переписка; медиа во вложениях нет.
- [x] Проверены три standalone-модуля из описания: `conptyreconcile`, `icons` и `wine_syscall_probe`.
- [x] Для `icons` и `wine_syscall_probe` добавлены правила `.gitignore` и для Unix-бинарей, и для Windows `.exe`.
- [ ] GitHub Actions для PR должны подтвердить корректность изменения.
- [ ] Тикет остаётся открытым до пользовательской проверки; закрытие Луноботом не выполняется.

## Решения и инварианты

- Правила остаются явными и ограниченными именами бинарей трёх существующих standalone-модулей; исходные файлы и артефакты других инструментов не скрываются широким шаблоном.
- Для каждого из трёх модулей игнорируются обе формы результата `go build`: имя без расширения на Unix и то же имя с `.exe` на Windows.
- Найденная в тикете ошибка `tools/icons/main_test.go` с отсутствующим `../../assets/icon/f4.svg` не входит в этот срез.

## Проверка

- Локальные сборки и тесты не запускались согласно инструкции Лунобота.
- Ожидаемая проверка через GitHub Actions: `git check-ignore` для `tools/icons/icons`, `tools/icons/icons.exe`, `tools/wine_syscall_probe/wineprobe` и `tools/wine_syscall_probe/wineprobe.exe`, а также отсутствие изменений для исходных файлов.
