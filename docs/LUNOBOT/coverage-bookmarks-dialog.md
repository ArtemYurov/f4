# Coverage: `cmd/f4/bookmarks_dialog.go`

## Status

- [x] Added focused tests for slot validation, sizing, rendering, persistence, movement, deletion, active-directory saving, and menu opening.
- [ ] Verify the coverage change in GitHub Actions.
- [ ] Merge the pull request after the required checks pass.

## Baseline

The Codecov snapshot used to select this file reported 9.49% line coverage for
`cmd/f4/bookmarks_dialog.go` and 60.31% total coverage on `main`.

Local builds and tests are intentionally not run; the repository passport
requires GitHub Actions as the build and test environment.
