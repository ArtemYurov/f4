# Coverage: `sdk/f4plugin`

## Status

The Codecov report for `main` at `83f501b2ebfefa3852f06a2faaa65cdaf7575f37`
reported 59.46% project coverage and 0% for `sdk/f4plugin`. This slice adds
an in-memory RPC harness covering the plugin server registration, base VFS
methods, optional command and panel providers, and all host callbacks exposed
by the SDK.

Local builds and tests were not run. GitHub Actions is the authoritative
verification for this change.
